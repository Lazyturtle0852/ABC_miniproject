# Streamlit secrets.toml ファイルの例
# このファイルをコピーして secrets.toml として保存し、実際のAPIキーを設定してください

# OpenAI API Key
OPENAI_API_KEY = "sk-your-actual-api-key-here"

# Supabase Database Connection (オプショナル)
# 対話履歴を永続化する場合は、Supabaseの接続情報を設定してください
# 設定しない場合でもアプリは正常に動作します（メモリのみモード）
# Supabase推奨のDATABASE_URL形式を使用（優先）
# DATABASE_URL = "postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres"

# 個別パラメータ形式（後方互換性のため、DATABASE_URLがない場合に使用）
# SUPABASE_DB_HOST = "db.xxxxx.supabase.co"
# SUPABASE_DB_PORT = "5432"
# SUPABASE_DB_NAME = "postgres"
# SUPABASE_DB_USER = "postgres"
# SUPABASE_DB_PASSWORD = "your-password-here"

# 使用方法:
# 1. このファイルを .streamlit/secrets.toml としてコピー
# 2. "sk-your-actual-api-key-here" を実際のAPIキーに置き換え
# 3. （オプション）Supabase接続情報を設定（対話履歴の永続化を有効にする場合）
#     - Supabaseプロジェクトの Settings > Database > Connection string から接続情報を取得
#     - DATABASE_URL形式を推奨（上記のコメントアウトを外して、実際の値を設定）
#     - または個別パラメータ形式を使用することも可能
# 4. このファイルは .gitignore に追加してください（機密情報を含むため）


